# Exercise 6 RAG Chatbot - Environment Variables Template
# =======================================================
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration (Required)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (Optional - uses defaults if not set)
# For local development with Docker:
DATABASE_URL=postgresql://rag_user:rag_password@localhost:5433/rag_chatbot

# Application Configuration (Optional - uses defaults)
ENVIRONMENT=development
DEBUG=true

# Additional Optional Settings (uses defaults if not provided):
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# OPENAI_TEMPERATURE=0.7
# OPENAI_MAX_TOKENS=1000
# SIMILARITY_THRESHOLD=0.5
# LOG_LEVEL=INFO
# CHROMADB_HOST=localhost
# CHROMADB_PORT=8000
